> 🔒 **ДЛЯ СЛУЖЕБНОГО ПОЛЬЗОВАНИЯ — CRYPTAVERITAS INTERNAL**
> Не подлежит распространению без разрешения руководства.

---

# Daily Plan — 2026-06-13

## Приоритеты дня

### 1. $VERAX — продолжение (HIGH)
```
[ ] Обновить index.html в cryptaveritas-verify
    (заменить CRYSIG → VERAX)
[ ] Создать $VERAX на Solana devnet (тест)
[ ] Настроить Squads v4 multisig кошелёк
[ ] OTC pre-launch механика (описать в docs)
[ ] Partner Booklet на русском языке
```

### 2. NodeMaven residential прокси (HIGH)
```
[ ] Купить план 2GB со скидкой VAN35 (~$5.52)
    https://nodemaven.com → Residential → 2GB
[ ] Проверить IP на IPQS (fraud score < 20)
[ ] Подключить в Incogniton профиль "Nexus"
[ ] Проверить pixelscan — No proxy detected
[ ] Подключить к Reddit/X аккаунтам
```

### 3. NexusVeritas pipeline (HIGH)
```
[ ] Max Degree Threshold в build_graph.js
    (защита от graph flood attack — 30 мин)
[ ] Масштаб до 1000+ creators
    (запустить enrich_parallel.js ночью)
[ ] candidate_discovery → MILESTONE_003
```

### 4. X @Runecipher137 (MEDIUM)
```
[ ] Проверить ответ X Support
    на runecipher.base@proton.me
[ ] Ответить если пришло новое письмо
```

### 5. Документация (LOW)
```
[ ] Обновить 2026-06-12.md (итоги дня)
[ ] Обновить PROJECT_PLAN.md статусы
[ ] Запустить end_of_day.sh
```

---

## Контекст из вчера

### $VERAX статус
```
Тикер:    $VERAX (проверен — свободен)
Значение: лат. "verax" = правдивый
Сеть:     Solana SPL
Supply:   100,000,000 VERAX
Multisig: Squads v4 (2 of 3)
IDO:      Jupiter LFG → Raydium (запасной)

Обновлено:
  cryptaveritas-roadmap  ✅
  cryptaveritas-verify/booklet.html  ✅
  VERAX_WHITEPAPER_v1.md  ✅

Осталось:
  cryptaveritas-verify/index.html  ❌
  Devnet тест  ❌
  Squads v4 настройка  ❌
```

### NexusVeritas статус
```
Creators:   565 (цель: 1000+)
Archetypes: 8
Pipeline:   enrich_parallel.js работает (5x)
API:        :3001 live
Similarity: P50=1.0 (collapse, нужны temporal vectors)
```

### Открытые риски
```
🔴 Graph flood attack — нет Max Degree Threshold
🔴 Similarity collapse P50=1.0
🟡 X @Runecipher137 — заблокирован
🟡 NodeMaven — не куплен
```

---

## Команды быстрого старта

```bash
# Мониторинг БД
docker exec nexusveritas-db psql -U nexus -d nexusveritas -c \
  "SELECT archetype, COUNT(*) FROM creators GROUP BY archetype ORDER BY COUNT(*) DESC;"

# Запуск pipeline
cd ~/Desktop/nexusveritas-api && \
  node enrich_parallel.js 2>/dev/null | grep '^{' | \
  node operator_classify.js 2>/dev/null | \
  node db_insert.js 2>/dev/null

# Сбор токенов
cd ~/Desktop/nexusveritas-api && \
  rm -f tokens_raw.txt && \
  for i in $(seq 1 20); do node find_tokens.js 2>/dev/null >> tokens_raw.txt; done && \
  awk '!seen[$1]++' tokens_raw.txt > tokens_clean.txt && \
  wc -l tokens_clean.txt

# NestJS API
cd ~/Desktop/nexusveritas-internal && npm run start:dev

# End of day
cd ~/Desktop/nexusveritas-api && bash end_of_day.sh
```
