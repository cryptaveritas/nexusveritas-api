# 2026-06-14 — Daily Report

## Что сделано

### Signal quality — главный фокус дня
1. **split_init_pattern реализован** (`3da95cf`) — последний из 8 сигналов.
   Детекция: ≥3 источника, avg < 0.5 SOL, окно ≤3 дня. Закрывает FINDING_002 (Hypothesis → Implementation).
   Замыкает самый тяжёлый сигнал ROTATION_OPERATOR (вес 0.35), который был мёртвым.

2. **Диагностика P50=1.0 collapse** — большое расследование:
   - Гипотеза "temporal vectors" отклонена — P50 измерялся как nearest-neighbor, не within-group
   - Подтверждено: 396/565 (70%) имеют best_sim ≥ 0.999, но 430 уникальных векторов — коллинеарность, не дубли
   - Корень: `tokens_created = floor(sigs/4)` упирается в потолок пагинации RPC (370 wallets на 3000 sigs, 88 на 1000)
   - 19/20 случайной выборки truncated даже на 5000 — population состоит из гипер-активных операторов

3. **getOps cursor pagination** (`df05b5e`) — снят потолок 1000/3000 sigs, курсор через `before`, до 10 страниц (10000 sigs), флаг `sigs_truncated`. Улучшил дифференциацию `days_active`/`launch_frequency`.

4. **vector v2.1** (`c50b290`) — заменены два сломанных компонента:
   - [10] `tokens_per_sig` (была константа ≈0.25 по построению) → log-scaled `activity_magnitude` (log1p(total_sigs)/log1p(20000))
   - [12] `mass_deployer` (насыщена 1.0 у 95% датасета) → log-scaled `sustained_activity_span` (log1p(days_active)/log1p(365))
   - `vector_version` → 'v2.1' для трекинга

### Инфраструктура
5. **MAX_DEGREE_THRESHOLD=50 + HIGH_DENSITY_TRANSIT** (`3d014ed`) — graph flood protection в build_graph.js
6. **recycling_loop** (`ff7c1bd`) — inc∩out пересечение источников финансирования
7. **sync_docs.sh** — автосинк docs/plan → nexusveritas-internal, встроен в end_of_day.sh

### Документы (в nexusveritas-internal/docs/)
- ARCHITECTURE_AUDIT.md — signal quality как корневая проблема
- VERAX_TOKENOMICS_AUDIT.md — аудит токеномики по booklet
- VERAX_MONETIZATION_AUDIT.md — Render/Helium/Chainlink сравнение, Howey-риски
- ROADMAP_2026_H2.md — план до конца года

### Прочее
- Создан приватный репо nexusveritas-internal (43MB, push успешен)
- Проверка всех 5 репозиториев после внешних изменений (CRYSIG→VERAX rename) — всё чисто, секретов нет
- Generator-скрипты убраны в scripts/ в verify и roadmap репо
- CoderLegion: статья VeritasLab опубликована + 3 комментария в обсуждениях
- Reddit: аккаунт NexusVeritas настроен, 2FA включён

## В прогрессе

- **Ре-enrichment 565 creators** на v2.1 — не выполнен, нужен для измерения эффекта фикса
- Калибровка порогов split_init_pattern — нужны реальные данные после ре-enrichment

## Завтра

1. **Полный ре-enrichment** существующих 565 creators через обновлённый pipeline (getOps + v2.1)
2. **Перемерить P50** — доказать что коллапс ушёл (цель: < 0.85)
3. Если коллапс остался — план Б: понизить веса бинарных флагов [3],[4],[6],[9],[11],[14],[16],[17],[18],[20]
4. NodeMaven residential прокси ($5.52, VAN35) — в фоне
5. matched_signals → NestJS API `/v2/scan/`
6. После стабилизации P50 — масштабирование до 1000+ creators

## Ключевая методологическая заметка

День показал ценность диагностики перед фиксом: первая гипотеза (temporal vectors, оценка "день работы") была неверной. Реальная причина — две конкретные строки кода (потолок пагинации + saturation clipping), найденные через 4 целевых SQL-запроса и 2 точечных RPC-теста. Дешевле и точнее, чем архитектурная переделка.

---

## Вторая половина дня (2026-06-14, доп.)

### Масштабирование 565 → 769 (+204, 36%)
5 раундов find_tokens -> enrich -> db_insert, все чистые (0 errors, 0 COLLAPSE_RISK):
565 -> 619 -> 663 -> 690 -> 723 -> 769

### Performance fix
CONCURRENCY 5->15 (b4548f1): 2.5x ускорение (24->48-61 токенов/мин), 0 ошибок.
CONCURRENCY=25 — throttling/hang, не использовать. Rust не требуется — bottleneck
это proxy/RPC latency (~650-1000ms/запрос, не CPU).

### top_funder (9342995)
Реализован, полный ре-enrichment проведён. funder_degree: p25=p50=1, p95=6, max=8.
Эффект на P50 минимальный (415->404 dupes) — разреженность при n=565.
Конвергенция с приоритетом масштабирования подтверждена.

### FINDING_006 (полный диагностический цикл P50)
6 гипотез проверены и отвергнуты эмпирически. Корень: недостаток признаков +
недостаток объёма для funder-overlap. P50=1.0, dupes=404/565 (71.5%).
Финальный статус: P50 переоценивается ПОСЛЕ масштабирования до 1000+.

### v3-аудит -> roadmap addendum
4 принципа интегрированы как guardrails (3-test validation, layer taxonomy,
collapse gate P50<0.85, future constraints для Q4). Задач не добавлено.

## Прогресс к 1000+
769/1000 (76.9%). При текущей скорости (~46-54 creators/раунд, ~2.5 мин/раунд)
осталось ~5 раундов (~12-15 минут).

## Завтра
1. Продолжить раунды масштабирования до 1000+
2. После 1000+ — переизмерить P50 + funder_degree (vector_lab.js)
3. Если P50<0.85 — Organizations API (matched_signals в /v2/scan/)
4. Если P50 высок — оценить funder_degree p95/max на новом объёме

---

## Финал дня (2026-06-14)

Масштабирование продолжено: 769 -> 800 (+31). Раунд 6 завис на 26 минут
(после успешной обработки 60/85 за 70с — единичный hang на residential proxy,
не системная проблема CONCURRENCY=15). Прерван, данные до точки зависания сохранены.

## Итог дня: 565 -> 800 creators (+41%)
6 раундов масштабирования, 5 чистых + 1 с зависанием (частично успешный).
Прогресс к 1000+: 800/1000 (80%).

## Завтра
1. Продолжить масштабирование (осталось ~200, при ~40-50/раунд — 4-5 раундов)
2. Если повторится зависание >5 мин на одном батче — добавить общий timeout
   на processLine целиком
3. После 1000+ — переизмерить P50 + funder_degree
4. P1 (HNSW индекс) — перед candidate_discovery работой
