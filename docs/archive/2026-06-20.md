# 2026-06-20 — Daily Report

## Что сделано

### split_init_pattern — фикс временного окна
Диагностирована проблема калибровки сигнала (реализован 14 июня, `3da95cf`):
`windowDays` считался как `(max_ts - min_ts)` по **всей** истории входящих
переводов, а не относительно момента первого деплоя токена.

**Проверка на проде (1027 creators) показала:**
PROFESSIONAL_CREATOR доминировал (20 случаев) над ROTATION_OPERATOR (17) —
неожиданно, учитывая что сигнал должен в основном указывать на rotation-паттерн.

**Фикс (`2bcb71d`):** getOps() теперь возвращает first_tx_ts, getFunding
ограничивает окно поиска split_init периодом перед первым деплоем токена.

**Тест на 51 свежем профиле:** 0 PROFESSIONAL_CREATOR false positives.

### Чистка репозитория
- Удалена дублирующаяся папка nexusveritas-api/nexusveritas-api/ (24MB)
- nexusveritas-internal: добавлен .gitignore, dist/ убран из tracking
- Удалён мусорный ARCHITECTURE_AUDIT.md (содержал "404: Not Found")
- VERAX_TOKENOMICS_AUDIT.md синхронизирован между репозиториями
- find-test-token.js закоммичен как dev-утилита

### Прокси
Старый residential proxy перестал отвечать, заменён на новый endpoint.

## Сверка с предыдущими находками
P50 collapse уже был закрыт 14-15 июня (FINDING_006) — глобальный P50
оказался неверной метрикой, per-archetype анализ показывает 90.3%
within-archetype match. PROJECT_PLAN.md не отражал этот прогресс,
обновлён сегодня отдельно.

## В прогрессе
- Расширенный тест split_init_pattern (100-200 записей)
- REENRICH существующих 1027+ записей
- Side findings 15 июня (RiskEngineV2Service мёртвый код, failing tests,
  funding-profiler TODO stubs) — всё ещё backlog

## Завтра
1. Расширенный тест + REENRICH
2. Решить судьбу side findings, особенно detectBurnerWallets
3. Закрепить ежедневный end_of_day.sh — не запускался 16-19 июня
