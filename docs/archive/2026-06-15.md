
---

## Утро 2026-06-15: Organizations API — первая интеграция

FINDING_006 закрыт (Addendum 3): vector design validated, 90.3% within-archetype
nearest-neighbor @ n=1012. Signal layer unblocked.

### Backfill (zero-RPC)
- ALTER TABLE creators ADD matched_signals text[], baseline_risk text
- db_insert.js патчен: сохраняет matched_signals/baseline_risk
- export_for_backfill.js: behavior JSONB -> classify -> db_insert, без RPC.
  Backfill всех 1012: has_signals=1012, has_baseline_risk=1012, 0 COLLAPSE_RISK

### nexusveritas-internal: deployer profile -> RiskEngineService (v1, active)
Обнаружено: RiskEngineV2Service существует, но НЕ подключён (мёртвый код).
Расширили активный v1.

- solana.adapter.ts: + creatorAddress: meta.creator?.address
- DeployerProfileService (новый): pg lookup creators по адресу деплоера
- RiskEngineService: + DEPLOYER_BASELINE_DELTA (high:+35, elevated:+20,
  neutral:0, unknown:0, low_medium:-5, low:-15) * confidence
  + DEPLOYER_SIGNAL_DELTA (split_init_pattern:+15, recycling_loop_true:+10,
  no_visible_funding:+5, fresh_wallet:+5, tokens_created_500_plus:+5,
  signatures_3000_plus:+5)
- scan.controller.ts: wire deployerProfile в calculateRisk
- main.ts: dotenv не был подключён -> добавлено

### Тест end-to-end
ROTATION_OPERATOR, confidence 0.85, signals: split_init_pattern, fresh_wallet,
tokens_created_500_plus: score 50 -> 90, isHardRefuse=true. Подтверждено вручную.

### Side findings (backlog, не сегодня)
- RiskEngineV2Service полностью не используется (мёртвый код)
- risk-engine.spec.ts: 4/8 тестов failing pre-existing
- funding-profiler.service.ts: BRIDGE_ADDRESSES placeholder-адреса,
  detectBurnerWallets — TODO стаб

Committed: nexusveritas-internal 4cc2c4e7
